# Dev overrides — builds from source instead of pulling the GHCR image:
# docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
# This file is for contributors and is not part of the release asset bundle.
services:
  app:
    build: .
  db:
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"
